export type CadastroForm = {
    nome: string;
    email: string;
    senha: string;
    confirmacao: string;
}

export type UsuarioPayload = {
    nome: string;
    email: string;
    senha: string;
}